tiktoken>=0.5.0
rapidfuzz>=3.0.0
fastapi
uvicorn

# This file lists the dependencies for the Token Cost Analyzer project. It includes:
# - tiktoken: A library for tokenizing text, which is essential for counting tokens in the input text.
# - rapidfuzz: A library for fuzzy string matching, used to find the closest model name based on user input.
# Make sure to install these dependencies using pip before running the Token Cost Analyzer. You can do this by running:
# pip install -r requirements.txt